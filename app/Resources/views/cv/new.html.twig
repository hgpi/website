{% extends 'base.html.twig' %}

{% block body %}
    <h1>Cv creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('fos_user_profile_show') }}">Retour à mon profil</a>
        </li>
    </ul>
{% endblock %}
